
SCHEMA >
    `age` Int16,
    `airline` String,
    `email` String,
    `extra_bags` Int16,
    `flight_class` String,
    `flight_from` String,
    `flight_to` String,
    `meal_choice` String,
    `name` String,
    `passport_number` Int32,
    `priority_boarding` UInt8,
    `seat_number` String,
    `timestamp` String,
    `transaction_id` String

ENGINE "MergeTree"
ENGINE_SORTING_KEY "age, seat_number, timestamp, transaction_id"
